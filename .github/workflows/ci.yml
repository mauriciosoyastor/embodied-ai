name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  quality:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
      - name: Install dev deps
        run: |
          pip install -e ".[dev,fase1]"
      - name: Lint
        run: ruff check .
      - name: Format
        run: ruff format --check .
      - name: Typecheck
        run: mypy fase-0
      - name: Tests
        run: pytest

  changes:
    runs-on: ubuntu-latest
    # Needed for dorny/paths-filter to compare against base
    permissions:
      pull-requests: read
    outputs:
      webcam: ${{ steps.filter.outputs.webcam }}
    steps:
      - uses: actions/checkout@v7
      - uses: dorny/paths-filter@v4
        id: filter
        with:
          filters: |
            webcam:
              - 'plataforma/webcam/**'

  webcam-ci:
    needs: changes
    if: ${{ needs.changes.outputs.webcam == 'true' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
      - name: Sync workspace
        run: uv sync --all-packages
      - name: Ruff check (webcam)
        run: uv run ruff check plataforma/webcam
      - name: Ruff format check (webcam)
        run: uv run ruff format --check plataforma/webcam
      - name: Mypy (webcam)
        run: uv run mypy plataforma/webcam
      - name: Pytest (webcam) headless
        run: uv run pytest plataforma/webcam -q
